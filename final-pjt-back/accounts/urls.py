from django.urls import path
from . import views

from django.urls import path
from . import views

app_name = 'accounts'
urlpatterns = [
    # path('users/', views.users),
    # path('signup/', views.signup),
    # path('myprofile/', views.마이프로필뷰),
    # path('follow/<int:my_pk>/<int:user_pk>/', views.팔로우뷰),
]